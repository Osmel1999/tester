#include "stdio.h"
#indlude "monty.h"

void char_extractor(*argv[]) {
char chr;
int count_lines = 0;
chr = &argv;
while (chr != EOF){
//count whenever new line is encountered
if (chr == 'n') {
count_lines = count_lines + 1;
}
printf("%s"chr);
}
}
